
## 实现SetStaticPath

@framework/mvc 中需要实现 SetStaticPath 功能。
类似：SetStaticPath("public","/static")
第一个参数是静态文件目录（相对应用所在的目录）
第二数是路径，url 路径信息，可选，如果没有这设置为当前路径
并在 @framework/mvc/app.go 中实现 HertzApp 的静态方法

```claudeCode
     目标

     在 @framework/mvc 中实现 SetStaticPath 功能，支持用户需求的API规范，并在 app.go 中实现 HertzApp 的静态方法。

     实现方案

     1. 修改 core/app.go 中的 SetStaticPath 方法

     - 当前问题: 现有方法 SetStaticPath(path string) 只接受一个参数
     - 新的实现: 重写为 SetStaticPath(localDir string, urlPath ...string) 
     - 功能特性:
       - 第一个参数: 静态文件本地目录（相对应用所在目录）
       - 第二个参数: URL路径（可选），如果不提供则自动推导
       - 支持 SetStaticPath("public", "/static") 和 SetStaticPath("public") 两种调用方式

     2. 在 app.go 中添加 HertzApp 静态方法

     - 添加 SetStaticPath 静态方法，调用 HertzApp 实例的方法
     - 确保API一致性和易用性

     3. 实现细节

     core/app.go 中的方法更新:
     func (app *App) SetStaticPath(localDir string, urlPath ...string) {
         if app.StaticPaths == nil {
             app.StaticPaths = make(map[string]string)
         }
         
         // 确定URL路径
         var targetUrlPath string
         if len(urlPath) > 0 && urlPath[0] != "" {
             targetUrlPath = urlPath[0]
         } else {
             // 自动推导: "public" -> "/public", "assets" -> "/assets"
             targetUrlPath = "/" + strings.TrimLeft(localDir, "./")
         }
         
         // 确保URL路径以/开头
         if !strings.HasPrefix(targetUrlPath, "/") {
             targetUrlPath = "/" + targetUrlPath
         }
         
         // 注册静态路径
         app.StaticPaths[targetUrlPath] = localDir
         app.Static(targetUrlPath, localDir)
     }

     app.go 中的静态方法:
     // SetStaticPath 设置静态文件路径的静态方法
     func SetStaticPath(localDir string, urlPath ...string) {
         if HertzApp != nil {
             HertzApp.SetStaticPath(localDir, urlPath...)
         }
     }

     4. 测试更新

     - 更新 app_test.go 中的测试用例以验证新API功能
     - 测试单参数和双参数调用场景
     - 验证静态方法的正确性

     优势

     1. 向后兼容: 支持原有的单参数调用方式
     2. 灵活性: 支持自定义URL路径或自动推导
     3. 易用性: 提供HertzApp静态方法，简化使用
     4. 一致性: 与现有的代码风格和架构保持一致
```

## 实现 AddFuncMap 函数

@framework/mvc 中需要实现 AddFuncMap 模板函数添加 功能。
类似：AddFuncMap("containString", tool.ContainString)
第一个参数：函数名字符串
第二个参数：函数实现
并在 @framework/mvc/app.go 中实现 HertzApp 的静态方法

```claudeCode

     📋 需求分析

     用户需要在 @framework/mvc 中实现 AddFuncMap 功能，用于添加自定义模板函数：
     - API: AddFuncMap("containString", tool.ContainString)
     - 第一个参数：函数名字符串
     - 第二个参数：函数实现

     🔍 现状分析

     通过代码分析发现：

     1. 现有模板系统架构：
       - view/engine.go 有完整的模板引擎实现，包含 AddFunction 方法
       - core/controller_template.go 控制器已有 AddTemplateFunction 方法  
       - 但缺少应用级别的全局模板函数管理
     2. 现有相关功能：
       - view/engine.go 中的 TemplateEngine.AddFunction(name string, fn any)
       - framework/util/format.go 中已有 ContainString 函数
       - view/template.go 中已有基础模板函数映射

     🎯 实现计划

     1. 在 core/app.go 中添加 AddFuncMap 方法到 App 结构体

     - 在 App 结构体中添加全局模板函数管理
     - 实现 AddFuncMap(name string, fn any) 方法
     - 将函数添加到所有模板引擎实例

     2. 在 app.go 中添加 AddFuncMap 静态方法供全局调用

     - 添加全局静态方法 AddFuncMap(name string, fn any)
     - 调用 HertzApp 实例的 AddFuncMap 方法

     3. 修改控制器的模板引擎初始化，支持全局自定义函数

     - 修改 core/template_engine_integration.go 
     - 确保控制器初始化模板引擎时加载全局自定义函数

     4. 在 view/engine.go 中添加全局 FuncMap 存储

     - 添加全局 funcMap 存储机制
     - 确保新创建的模板引擎实例都包含全局函数

     5. 编写测试用例验证 AddFuncMap 功能

     - 测试函数注册和调用
     - 测试在不同控制器中的函数可用性
     - 使用 tool.ContainString 作为测试案例

     6. 更新相关文档说明 AddFuncMap 使用方法

     - 在文档中添加 AddFuncMap API 说明
     - 提供使用示例和最佳实践

     🔧 技术细节

     1. 全局函数存储：使用 sync.RWMutex 保证线程安全
     2. 函数传播：确保添加的函数能传播到所有模板引擎实例
     3. 兼容性：保持与现有模板系统的兼容性
     4. 性能考虑：避免重复添加相同函数，优化模板编译

     📁 涉及文件

     - /Volumes/E/JYW/YYHertz/framework/mvc/core/app.go - 核心应用方法
     - /Volumes/E/JYW/YYHertz/framework/mvc/app.go - 全局静态方法
     - /Volumes/E/JYW/YYHertz/framework/mvc/core/template_engine_integration.go - 模板集成
     - /Volumes/E/JYW/YYHertz/framework/view/engine.go - 模板引擎
     - 测试和文档文件

     🎯 预期效果

     实现后用户可以这样使用：
     import "github.com/zsy619/yyhertz/framework/util"

     // 添加自定义模板函数
     mvc.AddFuncMap("containString", util.ContainString)
     mvc.AddFuncMap("customFunc", myCustomFunction)

     在模板中使用：
     {{if containString .Tags "important"}}
         <span class="important">重要内容</span>
     {{end}}

```

## 实现 InsertFilter 方法

判断实现如下方法的可能性：
实现 InsertFilter(pattern string, position int, filter FilterFunc, params ...bool)方法：
- pattern 路由规则，可以根据一定的规则进行路由，如果你全匹配可以用 *
- position 执行 Filter 的地方，五个固定参数如下，分别表示不同的执行过程
    1. BeforeStatic 静态地址之前  　　
    2. BeforeRouter 寻找路由之前
    3. BeforeExec 找到路由之后，开始执行相应的 Controller 之前
    4. AfterExec 执行完 Controller 逻辑之后执行的过滤器
    5. FinishRouter 执行完逻辑之后执行的过滤器
- filter filter 函数 type FilterFunc func(*mvc.context.Context)

```ClaudeCode

⏺ 基于我对现有代码架构的深入分析，我来评估 InsertFilter 方法的实现可行性。

  🔍 架构分析结果

  现有中间件架构优势

  YYHertz 框架已具备优秀的中间件基础设施：

  1. 完整的中间件管道系统 (middleware/pipeline.go):
    - 支持 4 个层级：LayerGlobal, LayerGroup, LayerRoute, LayerController
    - 具备优先级排序机制
    - 完整的统计和监控功能
    - 线程安全的实现
  2. 强大的中间件管理器 (middleware/manager.go):
    - 内置中间件工厂模式
    - 完整的注册表和元数据管理
    - 自动编译和热重载支持
    - 健康检查机制
  3. 增强的上下文系统 (context/context.go):
    - 对象池化支持
    - 完整的中间件执行链
    - Next() 和 Abort() 机制
    - 错误处理体系

  ✅ InsertFilter 实现可行性评估

  完全可行！ 基于现有架构，实现你要求的 InsertFilter 方法具有以下优势：

  1. 五个位置的技术映射

  你要求的 5 个过滤位置可以完美映射到现有系统：

  - BeforeStatic → 在静态文件处理之前 (新增层级)
  - BeforeRouter → LayerGlobal (全局层，路由匹配前)
  - BeforeExec → LayerRoute (路由层，控制器执行前)
  - AfterExec → LayerController (控制器层，执行后)
  - FinishRouter → 新增最终处理层级

  2. FilterFunc 类型兼容性

  你的 FilterFunc 类型 func(*mvc.context.Context) 与现有的 MiddlewareFunc 完全一致！

  3. 模式匹配支持

  现有架构已支持路由模式，可以轻松扩展支持 pattern 参数。

```

## Any等路由设置

### 需求
在 @framework/mvc/controller.go文件中 实现 在 @framework/mvc/router.go中实现路由注册：
  将 (group *RouterGroup) Any(relativePath string, handlers ...app.HandlerFunc) 转化为

  func Any(relativePath string, handlers ...HandlerFunc)         里面内容对接到 HertzApp.Any

### 初始执行

```claudeCode

    路由注册系统修复计划

     问题诊断

     当前 framework/mvc/controller.go:66 存在类型转换错误：
     - 自定义 HandlerFunc 与 Hertz 原生 app.HandlerFunc 类型不匹配
     - 需要正确的类型适配器来处理这种转换

     解决方案

     1. 修复 controller.go 中的类型问题

     - 移除当前有问题的 Any 函数实现
     - 导入正确的类型定义

     2. 创建 router.go 文件

     在 framework/mvc/router.go 中实现完整的路由注册系统：
     - 定义正确的 HandlerFunc 类型别名
     - 实现 Any() 及其他 HTTP 方法的路由注册函数
     - 提供类型适配器，正确转换 HandlerFunc 到 app.HandlerFunc

     3. 实现适配器模式

     - 创建 AdaptHandler 函数来处理类型转换
     - 确保增强的 Context 正确传递给处理函数
     - 支持过滤器系统集成

     4. 提供完整的路由API

     实现以下路由注册函数：
     - Any(relativePath string, handlers ...HandlerFunc)
     - GET/POST/PUT/DELETE/PATCH/HEAD/OPTIONS 等 HTTP 方法
     - 支持多个处理函数的链式调用

     5. 集成现有框架特性

     - 与统一常量系统集成
     - 与过滤器系统集成
     - 与增强的 Context 系统集成

     实现细节

     1. 修复类型定义和导入
     2. 创建路由注册文件
     3. 实现适配器函数
     4. 提供完整的 HTTP 方法支持
     5. 确保向后兼容性

     这个方案将解决类型转换问题，并提供一个完整、类型安全的路由注册系统。


  Update Todos
  ⎿  ☐ 修复controller.go中的类型问题
     ☐ 创建router.go文件实现路由注册
     ☐ 实现适配器模式
     ☐ 提供完整的HTTP方法支持
     ☐ 集成现有框架特性
```

### 迭代1

handlers 参数是否可以更改为 func(context.Context) 类型

```claudeCode

⏺ 通过分析代码，我发现将 handlers 参数改为 func(context.Context) 类型是完全可行的，而且有很多优势。让我分析一下这个方案：

  分析：使用 func(context.Context) 的优势

  1. 更符合Go标准实践

  - context.Context 是Go标准库的核心类型
  - 遵循Go社区的最佳实践

  2. 简化API设计

  - 减少参数数量，使API更简洁
  - 开发者只需要关心一个context参数

  3. 增强的Context可以通过context.Context获取

  - YYHertz的增强Context已经实现了完善的API
  - 可以通过context value机制传递增强Context

  4. 更好的类型安全

  - 避免了 *core.RequestContext 的类型转换
  - 使用标准的context.Context接口

  实现方案分析

  我看到YYHertz框架已经有了增强的Context系统(framework/mvc/context/context.go)，它提供了：

  - JSON/String等响应方法
  - 路由参数获取 (Param, Query, PostForm)
  - 请求头获取 (Header, GetHeader)
  - 键值对存储 (Set, Get, MustGet)
  - 中断控制 (Abort, IsAborted)
                                                                                                                                   
⏺ User approved Claude's plan:
  ⎿  将路由API参数改为 func(context.Context) 的实现方案

     方案概述

     将当前的 func(context.Context, *core.RequestContext) 改为 func(context.Context)，通过context.Context传递YYHertz的增强Context。

     实现策略

     1. 创建Context传递机制

     - 定义一个context key来在context.Context中传递增强的Context
     - 提供便捷的从context.Context获取增强Context的函数
     - 确保类型安全和性能

     2. 修改路由注册API

     更新所有HTTP方法签名：
     // 从:
     func GET(relativePath string, handlers ...func(context.Context, *core.RequestContext))

     // 改为:
     func GET(relativePath string, handlers ...func(context.Context))

     3. 更新适配器函数

     修改 AdaptHandlerToHertz 函数：
     - 将增强Context注入到context.Context中
     - 调用处理函数时只传递context.Context
     - 保持过滤器系统的完整性

     4. 提供便捷的Context获取API

     func FromContext(ctx context.Context) *Context
     func MustFromContext(ctx context.Context) *Context

     优势分析

     开发体验改善

     - API更简洁: 只需一个参数 func(ctx context.Context)
     - 符合Go惯例: 使用标准的context.Context
     - 类型安全: 避免手动类型转换

     使用示例对比

     // 当前方式
     mvc.GET("/users", func(ctx context.Context, c *core.RequestContext) {
         c.JSON(200, map[string]interface{}{"users": []string{}})
     })

     // 新方式
     mvc.GET("/users", func(ctx context.Context) {
         c := mvc.FromContext(ctx)
         c.JSON(200, map[string]interface{}{"users": []string{}})
     })

     框架集成

     - 保持与现有过滤器系统的完整兼容
     - 增强Context的所有功能依然可用
     - 向后兼容：可以同时支持两种API风格

     实现步骤

     1. 创建Context传递机制
       - 在mvc包中定义context key
       - 实现FromContext和MustFromContext函数
     2. 更新适配器函数
       - 修改AdaptHandlerToHertz实现
       - 将增强Context注入到context.Context
     3. 更新路由注册函数
       - 修改所有HTTP方法的签名
       - 保持返回值类型不变
     4. 更新文档和示例
       - 更新使用指南
       - 提供迁移指南

     向后兼容性

     - 可以提供两套API，逐步迁移
     - 或者提供迁移工具和文档
     - 保持现有控制器系统不变

     这个方案将使YYHertz的路由API更加现代化和符合Go社区标准，同时保持框架的强大功能。

  Update Todos
  ⎿  ☐ 创建Context传递机制   
     ☐ 更新适配器函数
     ☐ 修改路由注册API签名
     ☐ 提供便捷Context获取API
     ☐ 更新文档和示例

```

### 迭代2

handlers 参数是否可以更改为 func(contextenhanced.Context) 类型

```claudeCode

⏺ 分析完现有代码，我发现一个问题：你的想法很好，但直接使用 func(contextenhanced.Context) 会有一个类型传递的问题。让我看看现有的Context定义，然后制定一个更完善的计划。

  你想要的简化确实很有价值，从：
  mvc.SimpleGET("/path", func(ctx context.Context) {
      c := mvc.FromContext(ctx)
      c.JSON(200, data)
  })

  简化为：
  mvc.DirectGET("/path", func(c *contextenhanced.Context) {
      c.JSON(200, data)
  })

  这确实更直观！让我制定实施计划：
                                                                                             
⏺ User approved Claude's plan:
  ⎿  实现 func(contextenhanced.Context) 类型的DirectHandler API

     计划概述

     实现第三套API，支持直接传递 *contextenhanced.Context 的处理函数类型，进一步简化API使用。

     具体实施步骤

     1. 在 router.go 中新增 DirectHandlerFunc 类型

     // DirectHandlerFunc 直接接收增强Context的处理函数类型
     type DirectHandlerFunc func(*contextenhanced.Context)

     2. 创建 Direct 系列适配器函数

     // AdaptDirectHandlerToHertz 将Direct处理函数适配为Hertz app.HandlerFunc
     func AdaptDirectHandlerToHertz(handler DirectHandlerFunc) app.HandlerFunc

     3. 实现 Direct 系列HTTP方法

     创建完整的Direct API方法：
     - DirectAny, DirectGET, DirectPOST, DirectPUT, DirectDELETE
     - DirectPATCH, DirectHEAD, DirectOPTIONS

     4. 创建使用示例和文档

     - 创建 direct_api_demo.go 演示用法
     - 更新 SIMPLE_API_GUIDE.md 文档说明三套API的区别

     API进化对比

     1. 原始API: func(context.Context, *core.RequestContext) - 完整但复杂
     2. 简化API: func(context.Context) + mvc.FromContext() - 需要额外调用
     3. 直接API: func(*contextenhanced.Context) - 最简洁直观

     保持向后兼容

     所有现有API继续正常工作，只是新增Direct系列作为最简洁的选择。

     这个计划如何？

⏺ 好的，现在开始实现直接Context传递的API。

⏺ Update Todos
  ⎿  ☒ 创建Context传递机制                                       
     ☒ 更新适配器函数
     ☒ 修改路由注册API签名
     ☒ 提供便捷Context获取API
     ☒ 更新文档和示例
     ☐ 实现DirectHandlerFunc类型支持func(contextenhanced.Context)
     ☐ 创建Direct系列API方法(DirectGET,DirectPOST等)
     ☐ 更新适配器函数支持直接Context传递
     ☐ 创建Direct API使用示例
     ☐ 更新文档说明Direct API用法

```

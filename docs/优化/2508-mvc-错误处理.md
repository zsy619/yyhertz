## 错误处理

分析 beego 下的默认 错误处理机制，包括如何捕获和处理请求中的错误，以及如何返回友好的错误响应给客户端，错误处理controller源码是：
https://github.com/beego/beego/blob/master/server/web/error.go
进行深度分析理解应用，构建自己的默认错误处理机制，存储到：
@framework/mvc/error.go 文件，如何与App对象进行结合，并可运行客户端设置自己的错误处理器。

```claudeCode

     YYHertz框架错误处理机制设计方案

     📋 分析总结

     Beego错误处理机制核心特点

     1. 分层错误处理: 支持 errorTypeHandler 和 errorTypeController 两种类型
     2. 灵活注册机制: ErrorHandler() 注册函数处理器，ErrorController() 注册控制器处理器
     3. 回退机制: 支持多级错误处理回退（如503→500）
     4. 模板渲染: 自动渲染错误页面，包含详细错误信息
     5. 深度集成: 与路由系统和控制器生命周期完美集成

     YYHertz现状分析

     - 优势: 已有完善的错误类型定义(ErrNo)、恢复中间件、响应构建器
     - 不足: 缺乏统一的错误处理注册机制，无法自定义不同HTTP状态码的处理方式
     - 架构: 基于CloudWego Hertz，使用过滤器模式管理中间件

     🎯 设计方案

     1. 错误处理器架构设计

     // 错误处理器类型枚举
     type ErrorHandlerType int
     const (
         HandlerTypeFunction ErrorHandlerType = iota  // 函数处理器
         HandlerTypeController                        // 控制器处理器
         HandlerTypeTemplate                          // 模板处理器
     )

     // 错误处理器接口
     type ErrorHandler interface {
         Handle(ctx *RequestContext, statusCode int, err error)
         GetType() ErrorHandlerType
     }

     // 错误处理注册器
     type ErrorRegistry struct {
         handlers map[int][]ErrorHandler  // 状态码 -> 处理器列表
         fallbacks map[int][]int         // 回退映射 503->500->400
         mutex sync.RWMutex
     }

     2. 与App对象深度集成

     // App结构扩展
     type App struct {
         // 现有字段...
         errorRegistry *ErrorRegistry     // 错误处理注册器
         errorConfig   *ErrorConfig       // 错误配置
     }

     // 错误配置
     type ErrorConfig struct {
         ShowDetailedError bool                    // 是否显示详细错误
         DefaultErrorPage  string                 // 默认错误页面模板
         CustomErrorPages  map[int]string         // 自定义状态码页面
         LogErrors         bool                   // 是否记录错误日志
         EnableRecovery    bool                   // 是否启用panic恢复
     }

     3. 多层次错误处理

     // 函数处理器
     app.ErrorHandler(404, func(ctx *RequestContext, err error) {
         ctx.JSON(404, gin.H{"error": "Page not found"})
     })

     // 控制器处理器  
     app.ErrorController(500, &ErrorController{}, "InternalError")

     // 模板处理器
     app.ErrorTemplate(403, "error/403.html")

     4. 智能错误分发机制

     - 错误类型识别: 区分业务错误、系统错误、网络错误
     - 内容协商: 根据Accept头返回JSON、HTML、XML等格式
     - 环境适配: 开发环境显示详细堆栈，生产环境隐藏敏感信息
     - 国际化支持: 支持多语言错误消息

     5. 增强功能

     - 错误链追踪: 完整的错误传播路径记录
     - 性能监控: 错误频率和类型统计
     - 告警集成: 严重错误自动触发告警
     - 审计日志: 安全相关错误的审计记录

     📁 文件结构

     framework/mvc/error.go           # 核心错误处理实现
     framework/mvc/error/
     ├── registry.go                  # 错误处理注册器
     ├── handler.go                   # 各种错误处理器实现
     ├── config.go                    # 错误处理配置
     ├── middleware.go                # 错误处理中间件
     ├── template.go                  # 错误页面模板管理
     └── monitor.go                   # 错误监控和统计

     🔧 实现步骤

     1. 创建错误处理核心接口和结构
     2. 实现错误处理注册器
     3. 扩展App对象集成错误处理
     4. 实现各种错误处理器类型
     5. 创建默认错误处理器和模板
     6. 添加错误处理中间件
     7. 提供便捷的配置和使用API
     8. 编写测试用例和示例

     🎨 使用示例

     // 基础使用
     app := mvc.NewApp()

     // 注册函数处理器
     app.ErrorHandler(404, notFoundHandler)
     app.ErrorHandler(500, internalErrorHandler)

     // 注册控制器处理器
     app.ErrorController(403, &ErrorController{}, "Forbidden")

     // 设置错误配置
     app.SetErrorConfig(&mvc.ErrorConfig{
         ShowDetailedError: true,
         DefaultErrorPage: "error/default.html",
         LogErrors: true,
     })

     // 客户端自定义处理器
     app.CustomErrorHandler(func(ctx *RequestContext, statusCode int, err error) {
         // 自定义逻辑
     })

     这个方案借鉴了Beego的优秀设计，同时结合YYHertz框架的特点，提供了更加灵活和强大的错误处理能力。

  Update Todos
  ⎿  ☐ 创建错误处理核心接口和结构定义
     ☐ 实现错误处理注册器
     ☐ 扩展App对象集成错误处理功能
     ☐ 实现各种错误处理器类型
     ☐ 创建默认错误处理器和模板
     ☐ 添加错误处理中间件
     ☐ 提供便捷的配置和使用API
     ☐ 编写测试用例和示例

```

### 优化001

创建一个默认的  DefaultErrorController，提供一套默认的错误处理逻辑。
- 存储位置：@framework/mvc/error/default.go
- 页面模板参考 @framework/mvc/devtools/health_check.go 里面的模板页面。
- 提供默认的注册方法。

```claudeCode

     DefaultErrorController 实现方案

     我将创建一个完整的默认错误控制器，提供美观、功能齐全的错误处理体验。

     📋 实现内容

     1. DefaultErrorController 结构

     - 继承 BaseController，集成 MVC 框架特性
     - 支持多格式响应：HTML页面、JSON API、XML格式
     - 智能内容协商，根据请求类型自动选择响应格式
     - 配置化设计，支持调试模式和多语言

     2. 错误页面模板系统

     - 📱 响应式设计：基于 health_check.go 的现代化样式
     - 🎨 统一视觉风格：配色方案和布局保持一致
     - 🔍 状态指示器：不同错误类型使用不同颜色标识
     - 🛠️ 用户操作：提供返回首页、重试、联系支持等按钮

     3. 核心错误处理方法

     - Handle404() // 页面未找到
     - Handle401() // 未授权访问  
     - Handle403() // 禁止访问
     - Handle500() // 服务器内部错误
     - HandleGeneric(statusCode) // 通用错误处理

     4. 便捷注册方法

     - RegisterDefaultErrorHandlers(app) // 一键注册所有默认处理器
     - RegisterErrorController(app, statusCode) // 单独注册特定错误
     - SetErrorControllerConfig(config) // 配置错误控制器行为

     5. 特色功能

     - ✨ 开发友好：调试模式显示详细错误信息和堆栈跟踪
     - 🛡️ 生产安全：生产环境隐藏敏感信息
     - 🌍 国际化支持：中英文错误消息
     - 📊 上下文信息：请求路径、方法、时间戳等详细信息
     - 🔧 可扩展性：支持自定义错误页面和处理逻辑

     📁 文件结构

     framework/mvc/error/default.go
     ├── DefaultErrorController 结构体
     ├── 错误处理方法实现
     ├── HTML模板定义
     ├── 内容协商逻辑
     └── 便捷注册函数

     这个实现将为 YYHertz 框架提供企业级的错误处理体验，既美观又实用。

  Update Todos
  ⎿  ☐ 创建DefaultErrorController结构体和基础定义
     ☐ 实现HTML模板系统，参考health_check样式
     ☐ 实现核心错误处理方法(404,401,403,500)
     ☐ 实现内容协商和JSON/XML响应
     ☐ 创建便捷注册方法

```

### 优化002

将 @framework/errors/error.go 与 @framework/mvc/error/ 目录 与 @framework/mvc/error.go 文件  一同合并到 @framework/mvc/errors/目录，特别是要统一 ErrorHandlerFunc (ctx *RequestContext, statusCode int, err error) 签名，构建统一错误处理系统，分析可能性及相关工作

```claudeCode

  📊 当前错误处理系统结构分析

  🔍 现有系统组成

  1. @framework/errors/error.go - 基础错误处理接口和类型定义
  2. @framework/mvc/error.go - MVC特定的错误处理实现
  3. @framework/mvc/error/ 目录 - 详细的错误处理模块
  4. @framework/mvc/errors/ 目录 - 高级错误分类和恢复系统

  ⚠️ 关键问题识别

  ErrorHandlerFunc 签名不统一：
  - @framework/errors/error.go: func(ctx RequestContext, statusCode int, err error) error
  - @framework/mvc/error.go: func(ctx *RequestContext, statusCode int, err error) error
  - @framework/mvc/errors/handler.go: func(ctx *mvccontext.Context, err error) error

  架构层次混乱：
  - 三个不同的错误处理体系并存
  - 类型定义重复且不兼容
  - 依赖关系复杂，存在循环引用风险

  🎯 统一ErrorHandlerFunc签名设计方案

  📝 推荐的统一签名

  type ErrorHandlerFunc func(ctx *mvccontext.Context, statusCode int, err error) error

  选择理由：
  1. 具体类型 - 使用*mvccontext.Context避免interface{}的类型断言
  2. 完整参数 - 保留statusCode参数，支持HTTP状态码处理
  3. 向后兼容 - 与现有MVC框架集成度最好
  4. 性能最优 - 避免不必要的类型转换

  🏗️ 目录合并策略

  🎯 目标结构：@framework/mvc/errors/

  @framework/mvc/errors/
  ├── interfaces.go      # 统一接口定义
  ├── handlers.go        # 基础错误处理器实现
  ├── registry.go        # 错误注册器
  ├── config.go         # 错误配置管理
  ├── middleware.go     # 错误处理中间件
  ├── recovery.go       # 自动恢复系统 (现有)
  ├── classifier.go     # 智能分类系统 (现有)
  ├── default.go        # 默认错误控制器
  ├── examples.go       # 使用示例
  └── utils.go          # 工具函数

  🔧 技术实施方案

  1️⃣ 接口统一设计

  // 核心错误处理器接口
  type ErrorHandler interface {
      Handle(ctx *mvccontext.Context, statusCode int, err error) error
      CanHandle(statusCode int, err error) bool
      GetPriority() int
      GetType() ErrorHandlerType
  }

  // 统一的错误处理函数类型
  type ErrorHandlerFunc func(ctx *mvccontext.Context, statusCode int, err error) error

  // 错误注册器接口
  type ErrorRegistry interface {
      RegisterHandler(statusCode int, handler ErrorHandler) error
      RegisterHandlerFunc(statusCode int, handler ErrorHandlerFunc) error
      HandleError(ctx *mvccontext.Context, statusCode int, err error) error
      GetStats() map[string]any
  }

  2️⃣ 兼容性适配器

  // 为旧签名提供适配器
  func AdaptLegacyHandler(oldHandler func(*RequestContext, int, error) error) ErrorHandlerFunc {
      return func(ctx *mvccontext.Context, statusCode int, err error) error {
          return oldHandler(ctx.RequestContext, statusCode, err)
      }
  }

  // 为简化签名提供适配器  
  func AdaptSimpleHandler(simpleHandler func(*mvccontext.Context, error) error) ErrorHandlerFunc {
      return func(ctx *mvccontext.Context, statusCode int, err error) error {
          return simpleHandler(ctx, err)
      }
  }

  3️⃣ 集成现有高级功能

  // 集成智能分类和自动恢复
  type UnifiedErrorSystem struct {
      registry   ErrorRegistry
      classifier *IntelligentClassifier
      recovery   *AutoRecovery
      config     *ErrorConfig
  }

  func (u *UnifiedErrorSystem) ProcessError(ctx *mvccontext.Context, statusCode int, err error) error {
      // 1. 错误分类
      classification := u.classifier.Classify(err, ctx)

      // 2. 尝试自动恢复
      if classification.Retryable {
          result := u.recovery.Recover(ctx, err)
          if result.Success {
              return nil
          }
      }

      // 3. 委托给注册的处理器
      return u.registry.HandleError(ctx, statusCode, err)
  }




     🎯 ErrorHandler系统统一合并计划

     📋 核心目标

     将@framework/errors/error.go、@framework/mvc/error/目录和@framework/mvc/error.go文件合并到@framework/mvc/errors/，构建统一的错误处理系统。

     🔧 关键工作项

     1. 统一ErrorHandlerFunc签名 ⭐
     - 采用：func(ctx *mvccontext.Context, statusCode int, err error) error
     - 创建兼容适配器支持现有代码
     - 避免破坏性更改

     2. 重新组织文件结构
     @framework/mvc/errors/
     ├── interfaces.go      # 统一接口定义  
     ├── handlers.go        # 错误处理器实现
     ├── registry.go        # 注册器系统
     ├── config.go         # 配置管理
     ├── default.go        # 默认控制器
     ├── middleware.go     # 中间件集成
     ├── classifier.go     # 智能分类(保留)
     ├── recovery.go       # 自动恢复(保留)
     └── utils.go          # 工具函数

     3. 代码迁移策略
     - 保留现有高级功能(分类器、恢复系统)
     - 合并重复的错误处理器实现
     - 统一配置管理系统
     - 创建适配层确保向后兼容

     4. 集成验证
     - 更新所有import路径
     - 测试现有功能完整性
     - 验证性能无回退
     - 确保API兼容性

     ⚡ 优势

     - 统一架构：消除重复和混乱
     - 性能优化：减少类型转换开销  
     - 维护性：集中化错误处理逻辑
     - 可扩展：保留高级功能如智能分类
     - 兼容性：不破坏现有代码

     🛡️ 风险控制

     - 渐进式迁移，分批更新
     - 完整的单元测试覆盖
     - 保留兼容适配器
     - 详细的迁移文档

     预计影响范围：约15-20个文件，预计完成时间：2-3小时

```

### 优化003

继续合并，并删除 @framework/errors/error.go 与 @framework/mvc/error/ 目录,修订相关错误


### 优化004

拆分,文件 @framework/mvc/core/app.go 按照功能拆分成多个文件,文件以 app_ 开头来命名。


## 错误处理文档

完善帮助文档，根据 @framework/mvc/errors 的内容，编写错误处理的使用文档，存储到：
- 帮助文档：markdown格式，存储位置 @example/simple/docs/ 目录下或子目录下
- 栏目设置：@example/simple/views/shared/docs-navigation.html ， 放到合适的栏目下，是否可以拆分成多个子栏目
- 路由定义：@example/simple/controllers/docs_controller.go
- 路由设置：@example/simple/main.go
要求：
- 充分分析simple项目的文档展示方式，对错误处理进行梳理，充分完善错误处理文档，放到合适目录下。
- 完善包括默认路由设置、自定义路由设置等但不限于这些内容

```claudeCode

🚨 YYHertz 错误处理文档完善计划

     📋 分析结果

     经过对 framework/mvc/errors 包和现有文档结构的深入分析，发现：

     🔍 错误处理框架特性

     - 统一错误处理系统 - 包含智能分类、自动恢复、错误注册分发
     - 默认错误控制器 - 支持15+种HTTP状态码，美观错误页面
     - 多种响应格式 - HTML/JSON/XML，智能内容协商
     - 企业级功能 - 错误监控、统计、业务错误码、自动恢复

     📁 现有文档结构特点

     - 使用Bootstrap手风琴导航
     - 支持三级导航结构
     - 统一的Markdown渲染系统
     - 完善的路由配置机制

     🎯 文档结构设计

     📂 目录规划

     docs/error-handling/
     ├── overview.md              # 错误处理概览
     ├── quick-start.md           # 快速开始
     ├── default-handlers.md      # 默认处理器
     ├── custom-handlers.md       # 自定义处理器
     ├── error-pages.md           # 错误页面定制
     ├── monitoring.md            # 错误监控
     ├── business-errors.md       # 业务错误码
     ├── recovery.md              # 自动恢复机制
     ├── best-practices.md        # 最佳实践
     └── troubleshooting.md       # 故障排除

     🎨 导航栏位置

     将错误处理放入 "MVC核心" 栏目，位于控制器和路由系统之后，与框架架构保持一致。

     🛣️ 路由配置

     - 路径前缀：/docs/mvc-core/error-handling/
     - 控制器方法：GetMvcCoreErrorHandling*
     - 支持子路由导航高亮

     📚 文档内容规划

     1. 📖 概览文档 (overview.md)

     - 错误处理系统架构图
     - 核心组件介绍（Registry、Controller、Dispatcher）
     - 主要特性和优势
     - 版本信息和API兼容性

     2. 🚀 快速开始 (quick-start.md)

     - 5分钟上手指南
     - QuickSetup快速配置
     - 常用错误处理场景代码示例
     - 开发环境vs生产环境配置

     3. 🎛️ 默认处理器 (default-handlers.md)

     - DefaultErrorController功能详解
     - 支持的HTTP状态码（400-505）
     - 错误页面模板系统
     - 多语言支持配置

     4. 🔧 自定义处理器 (custom-handlers.md)

     - 自定义错误处理器开发
     - ErrorHandler接口实现
     - 处理器优先级和注册
     - 错误回退机制

     5. 🎨 错误页面定制 (error-pages.md)

     - HTML模板自定义
     - CSS样式个性化
     - JavaScript交互功能
     - 响应式设计支持

     6. 📊 错误监控 (monitoring.md)

     - 错误统计和指标收集
     - 监控告警配置
     - 性能分析工具
     - 日志记录策略

     7. 💼 业务错误码 (business-errors.md)

     - BusinessError结构设计
     - 错误码规范定义
     - 国际化错误消息
     - API错误响应格式

     8. 🔄 自动恢复机制 (recovery.md)

     - AutoRecovery系统原理
     - 恢复策略配置
     - 可重试错误类型
     - 熔断器模式实现

     9. ⭐ 最佳实践 (best-practices.md)

     - 错误处理设计原则
     - 性能优化建议
     - 安全考量
     - 测试策略

     10. 🛠️ 故障排除 (troubleshooting.md)

     - 常见问题及解决方案
     - 调试工具使用
     - 错误诊断流程
     - FAQ常见问题

     🔧 技术实现

     📝 文件创建

     - 创建10个Markdown文档文件
     - 包含完整的代码示例和配置说明
     - 支持语法高亮和交互式演示

     🎯 导航更新

     - 在MVC核心栏目新增"错误处理"二级导航
     - 支持子路由折叠展开
     - 添加错误处理图标（⚠️）

     🛣️ 路由配置

     - 在DocsController添加10个处理方法
     - 在main.go中配置对应的NSRouter
     - 保持与现有路由风格一致

     🎨 样式适配

     - 复用现有Bootstrap样式
     - 错误处理专用图标和配色
     - 保持文档视觉一致性

     这个计划将为YYHertz框架提供业界领先的错误处理文档，覆盖从入门到高级的所有使用场景。

```


## 2508-0001 数据库-gorm

目录 @framework/orm 是基于golang gorm框架的二次封装，可以简单、方便、快速的完成基本的增删改查功能。
请参考gorm原始项目 https://github.com/go-gorm/gorm 进行封装优化。

```kimi
你是一名资深 Golang 架构师。现在有一份代码位于目录 @framework/orm，它是基于 GORM 的二次封装，目标是用最简 API 完成日常 CRUD。

请按以下 5 个步骤输出（标题即步骤名，不要额外解释）：

Step-1  现状梳理  
- 用 bullet list 概括 @framework/orm 目前暴露的 3-5 个核心 API（函数签名即可）。  
- 指出其中「最冗余」或「最易误用」的 1-2 处设计。

Step-2  差距对比  
- 以「官方 GORM」为基准，列出 5-7 个「官方已内置、但当前封装缺失」且对「简单、方便、快速」目标影响最大的特性。  
- 用 High / Medium / Low 标注优先级。

Step-3  优化方案  
- 针对 High & Medium 级差距，给出在「不改 GORM 源码」前提下的最小侵入式改进方案。  
- 每个方案 ≤ 4 行文字，须包含：设计思路、新增/修改的文件、对现有调用方的影响。

Step-4  代码示例  
- 用 Golang 1.22 + GORM v1.25.x，为「新增分页 & 自动时间戳钩子」各提供一段 15 行以内的可运行示例。  
- 示例需直接放入 @framework/orm 目录即可编译（保留包名 `orm`）。

Step-5  迁移清单  
- check list 形式列出：需新建的文件、需改动的旧文件、单测新增位置、回归验证点（3 条以内）。  
```

## 2508-0002 文档

1.分析 @framework/orm 这个文件夹命名是否合适
2.如不合适给出合理的命名，如果多个，给出列表供选择
3.根据功能生成 @framework/orm/README.md 文档

## 2508-0004 完善示例

2.分析 @framework/orm 里面 md文档及功能，完善 @sample/ormx 示例，给出必要的性能测试、压力测试
3.生成 @sample/ormx/README.md 文档

## 2508-0003 帮助文档

依据 @framework/orm/ 下md文档 及 本功能特点 及 @sample/ormx 下的示例与md文档，还有 @conf/database.yaml 配置文件，做如下优化：
1.修订 @README.md 文档
2.分析 @example/simple/views/home/docs.html 文档中【GORM集成】栏目是否拆分成多个
2.1 如拆分则给出合理的命名，并完成相关栏目与内容的调整。包括 @example/simple/views/home/docs.html 与 @example/simple/views/home/docs/docs-sidebar.html 相关导航
2.2 修订 @example/simple/docs/data-access/orm.md 文档
2.3 修订相关引用链接


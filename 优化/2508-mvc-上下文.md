## Context 文件拆分计划

对 @framework/mvc/context/context.go 文件进行拆分:按功能拆分多个文件;其中Context结构体,按功能拆分为多个文件,文件以context_开头

```claudeCode

  1. Engine字段处理：Context结构体包含engine *Engine字段（第96行），需要确保Engine类型在拆分后正确引用
  2. 依赖管理：
    - 保持现有的import声明
    - 确保各文件间的依赖关系正确
    - 避免循环依赖
  3. 现有增强功能保护：
    - 保持现有的context_enhanced.go文件不变
    - 确保增强功能与拆分后的基础功能兼容
  4. 编译验证：
    - 确保拆分后所有文件都能正常编译
    - 验证multi_handlers示例仍能正常工作
  5. 类型安全：
    - 维护现有的类型安全操作
    - 保持sync.Map的并发安全性
                                     


     Context.go 文件拆分计划

     将 /Volumes/E/JYW/YYHertz/framework/mvc/context/context.go (763行) 按功能拆分为8个文件：

     拆分文件结构：

     1. context_core.go - 基础类型和Context结构定义 (13-118行)
     2. context_keys.go - 键值对操作 (162-368行)  
     3. context_typed.go - 类型安全操作 (209-560行)
     4. context_errors.go - 错误处理 (369-416行)
     5. context_params.go - 路由参数处理 (435-492行)
     6. context_lifecycle.go - 池化和生命周期 (120-161, 494-505行)
     7. context_io.go - I/O操作 (562-731行)
     8. context_compat.go - 兼容性访问器 (417-434, 506-529行)

     关键处理点：

     - 保护Engine字段引用 (第96行)
     - 维护现有import和依赖关系
     - 保持context_enhanced.go文件不变
     - 确保编译正常和multi_handlers示例工作
     - 维护sync.Map并发安全性
                                    
```

## OutputData、InputData

分析beego项目中 func (output *BeegoOutput) Download(file string, filename ...string)  实现方法,请完善@framework/mvc/context/adapter.go 中  OutputData结构体完善Download方法,看还有哪些实用方法也移植过来


分析 beego 的 BeegoInput 实用方法，移植到 InputData 结构体中


按结构体拆分文件：@framework/mvc/context/adapter.go


## core BaseController

深度分析 beego 下 https://github.com/beego/beego/blob/8fd113aa0937a11c45eb41cbf147f33df7e9fb6f/server/web/controller.go 中 ParseForm 、GetStrings 实现机制，完成BaseController对应方法，放到文件 @framework/mvc/core/controller_request.go 文件中



充分分析beego框架中controller实现,完成 @framework/mvc/core/controller_request.go 中  BaseController 结构体
1.实现对应的Bind、BindYAML、BindForm、BindJSON、BindProtobuf等方法，注意不要与BaseController结构体下重复
2.修订ReservedMethods变量



充分分析beego框架中controller实现,完成 @framework/mvc/core/controller_request.go 中  BaseController 结构体
1.实现对应的GetInt8、GetInt16、GetUint8、GetUint16、GetUint32、GetUint64等方法，注意不要与BaseController结构体下方法重复
2.修订ReservedMethods变量



完成 @framework/mvc/core/controller_request.go 中  BaseController 结构体:
1.实现GetMap，获取 form 的值，返回 map[string] any 
2.定义 GetMap(exclude ...string) (data map[string]any) 方法，支持排除指定字段
3.修订ReservedMethods变量



充分分析beego框架中controller实现,完成 @framework/mvc/core/controller_request.go 中  BaseController 结构体 的 SaveToFile 等方法，
对 @framework/mvc/core/controller_request.go 中  BaseController 结构体 进行如下操作：
1.实现SaveToFile，支持将文件保存到指定路径
2.新增一个获取request请求体的方法：GetRequestBody
3.修订ReservedMethods变量

